version: '2.2'
services:
    mimedefang:
        container_name: mimedefang-postfix
        image: mimedefang/postfix:v2.86
        working_dir: /tests
        volumes:
         - .:/tests
        command: sh t/dockerPostfix.sh
