# Python modules needed by mimedefang-gliclass-server.
#
# To avoid the multi-GB CUDA build of torch on a CPU-only host, install
# torch from the CPU wheel index first (see README.md), then:
#
#   pip install -r requirements-gliclass.txt
gliclass
transformers
torch
flask
waitress
